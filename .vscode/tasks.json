{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "catkin build",
            "type": "shell",
            "command": "source /opt/ros/melodic/setup.bash && catkin build && source devel/setup.bash",
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}